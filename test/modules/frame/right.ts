export const RIGHT = 2;

// Same names as left.ts, different types (numbers here, strings there).
for (const item of [10, 20]) {
  console.log(item * RIGHT);
}

if (RIGHT > 1) {
  const tmp = 99;
  console.log(tmp);
}
