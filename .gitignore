node_modules/
bun.lockb
# Compiler output
*.ll
# but the fixed toolchain-smoke input must be tracked (present in CI/worktrees)
!test/assets/*.ll
*.o
*.out
/build/
/.nativets-cache/
# OS
.DS_Store

# compiled single-executable
/nativets

# git worktrees for parallel agents (managed by git, not tracked)
.claude/worktrees/
